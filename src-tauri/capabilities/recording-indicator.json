{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-indicator",
  "description": "Capability for the floating recording indicator overlay window",
  "windows": ["recording-indicator"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-hide",
    "core:window:allow-close"
  ]
}
